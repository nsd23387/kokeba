services:
  api: { build: ../../apps/api }
  worker: { build: ../../apps/worker }
  web: { build: ../../apps/web, ports: ["3000:3000"] }
  db: { image: postgres:16, environment: { POSTGRES_DB: kokeba } }
