# @kokeba/api — Orchestrator + Control Plane
Auth, job orchestration, approval gates, budget enforcement, logging, kill switch,
license entitlement (which tiers are unlocked). Owns the catalog/tracker database.
